{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "koubo://schemas/memory-promotion/v1",
  "title": "Koubo Memory Promotion v1",
  "type": "object",
  "x-koubo-common-record": true,
  "required": [
    "id", "schemaVersion", "createdAt", "createdBy", "status", "source", "evidence",
    "applicability", "prohibitions", "versions", "contentHash",
    "recordKind", "recordId", "fromStatus", "toStatus", "actor", "reason"
  ],
  "properties": {
    "schemaVersion": { "type": "integer", "enum": [1] },
    "status": { "type": "string", "enum": ["recorded", "rolled-back"] },
    "recordKind": { "type": "string" },
    "recordId": { "type": "string" },
    "fromStatus": { "type": "string" },
    "toStatus": { "type": "string" },
    "actor": { "type": "object" },
    "reason": { "type": "string" }
  },
  "additionalProperties": true
}
